import clsx from "clsx";

export function Footer() {
  return (
    <footer
      className={clsx(
        "container mx-auto bg-[var(--navbar)] h-20 px-6 flex items-center justify-between",
      )}
    >
      footer
    </footer>
  );
}
